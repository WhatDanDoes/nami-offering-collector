<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="description" content="<%= process.env.TITLE %>">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= process.env.TITLE %></title>
    <link rel='stylesheet' href='/stylesheets/mvp.css' />
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <header>
      <nav>
        <ul>
          <li id="authenticate"></li>
        </ul>
      </nav>
    </header>
    <main>
      <h1><%= title %></h1>
      <p>Welcome to <%= title %></p>
    </main>
    <script>
      document.addEventListener('DOMContentLoaded', function(event) {
        var login = document.getElementById('authenticate');
        if (window.web3) {
          login.innerHTML = 'Authenticate with Metamask';
        }
        else {
          login.innerHTML = 'Install the Metamask browser plugin';
        }
      });
    </script>
  </body>
</html>
